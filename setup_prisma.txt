--commandes à entrer dans un terminal normal--

cd
sudo apt install postgresql
sudo -u postgres psql template1
create user %nom utilisateur% createdb encrypted password '%mot de passe%';
exit
createdb %nom base de données%

-récupérer le fichier de backup sur notion (documents => backup bdd)
-copier le fichier sur WSL

psql %nom bdd% < %nom fichier backup%


--commandes à entrer dans le terminal VSCode--

-créer un fichier .env dans la racine du projet et mettre les infos de la BDD postgres :
DATABASE_URL="postgresql://%nom utilisateur%:%mot de passe%@localhost:5432/%nom bdd%?schema=public"

npm install prisma
npx prisma db pull
npx prisma generate
npm run dev